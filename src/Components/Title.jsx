import React from 'react'

const Title = ({title}) => {
  return (
    <div>
        <h1 className='font-Integral text-[20px] sm:text-[35px] lg:text-[48px] font-bold text-[#000000] text-center uppercase'>{title}</h1>
    </div>
  )
}

export default Title